> The Micronaut Kubernetes project provides integration between Micronaut and Kubernetes.
It adds support for the following features:
* Service Discovery
* Configuration client for config maps and secrets
* Kubernetes blocking and non-blocking clients built on top of the official Kubernetes Java SDK